//JS enfocado en renderizar elementos generales en todos los HTML

//Render para navbar
export function renderizarNavbar (ruta, contenedor)
{
    contenedor.innerHTML= "";

    //Verificamos la ruta o ruta porfecto
    if (xx) {
        
    }

    contenedor.innerHTML= ``; //codigo navbar

    //apicar el modo oscuro
    // if (condition) {
    //     contenedor.innerHTML= renderizarNavbar(./, main,true)`HTML del modo oscuro`;
    // }
    // else{
    //     contenedor.innerHTML= `html del modo claro`;
    // }
}
